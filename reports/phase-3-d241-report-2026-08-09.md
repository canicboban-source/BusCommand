# FAZA 3 D24.1 — Security + transactional proof correction (2026-08-09)

**Base SHA:** `a6fbcb508c67287c33479f38c3678cd44684ee60`  
**Verdict:** D24.1 **CLOSED** for EID isolation + transactional assignment revalidation + executable proofs.  
**STOP:** no commit / push / deploy / next phase. Dirty tree preserved.

---

## Critical finding 1 — EID leak (fixed)

| Before | After |
|--------|--------|
| Manual create wrote `eid` onto `drivers/{id}` | Profile write has **no** `eid` / PIN / hash |
| GET list backfilled `eid` onto profiles | GET merges EID from `driver_credentials` into **API response only** |
| Dispo could read dirty profile docs | Rules deny Dispo read when credential fields present; clean profiles readable; CA own-tenant unchanged |

Proof:
- Emulator Rules: sentinel EID profile **fails** for Dispo; clean succeeds; CA reads dirty.
- `createManualCompanyDriver` → profile without eid; credentials hold eid; list does not backfill.
- Migration dry-run / `buildMigrationPlan` proves legacy strip path (no live apply).

## Critical finding 2 — Stale resource race (fixed)

Assignment `PUT /api/staff/shifts/assignment` now re-reads inside the mutation transaction (all before first write):
- driver + staff scope
- bus query (active/opsStatus/pool)
- active duty catalog (`getActiveServicePlanInTx`)
- conflict shifts + import lock + revision

Stale bus opsStatus flip → `409 BUS_NOT_AVAILABLE`, zero writes (emulator HTTP).

## Proof gap — executable (not regex-only)

Rules suite now **137/137** (was 122; count increased).

Assignment HTTP (emulator): BUS_NOT_FOUND / INACTIVE / NOT_AVAILABLE / OUTSIDE_GROUP / DOUBLE_BOOKED / stale bus / duty missing / time mismatch / REVISION_CONFLICT / valid canonical write.

CA: unauthorized dispatcher + cross-tenant denied; create success profile+credentials together; audit sanitize drops eid/PIN/OTP.

### Owner STOP (parallel uniqueness)

Parallel EID / license races across **different new document IDs** cannot be serialized without a shared reservation document (**new schema**). Not implemented. Sequential `EID_EXISTS` / `DRIVER_LIMIT_REACHED` remain server-authoritative. Needs explicit owner decision before any reservation schema.

---

## D17 (single clean build)

| Metric | Value | Budget |
|--------|------:|-------:|
| staff JS excl. translations | **577311** | 581632 |
| translations | **342341** | 377856 |

No budget bump.

---

## Final gates (EXIT 0)

| Gate | Result |
|------|--------|
| secrets | 0 |
| lint | 0 |
| unit full | **757 PASS** (↑ from 753) |
| HTTP/auth | **47 PASS** (↑ from 44) |
| Rules emulator | **137/137** (↑ from 122) |
| E2E full | **105 PASS** |
| firebase isolation | 0 |
| audit (`--omit=dev`) | 0 vulns |
| build + D17 | 0 |
| visual trail (UI-only/QA-local) | 0 |

Logs: `reports/phase-3-d241-logs/`  
Fail-first unit: `fail-first-unit.txt`

---

## Visual

Existing `reports/phase-3-visual/` retained and labeled **UI-only / QA-local** in README.  
Server / Rules / credential / race proofs are executable tests, not screenshots.

---

## Packaging

- `reports/phase-3-d241-source-manifest.txt`
- `reports/phase-3-d241-review-source-2026-08-09.zip`
- `reports/phase-3-d241-full-deliverable-2026-08-09.zip`
- `phase-3-d241-logs/manifest-verifier.txt` → EXIT=0

---

## Not done

- Commit / push / deploy
- Parallel uniqueness reservation schema
- Live credential migration apply
- Next phase
