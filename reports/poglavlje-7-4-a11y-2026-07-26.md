# Poglavlje 7.4 — Accessibility slice (2026-07-26)

## Šta je urađeno

1. **Skip link** na `staff.html` / `driver.html` → `#main-content`.
2. **Main landmark** `id="main-content" tabindex="-1"`.
3. **Icon-only**: theme, logout, SOS, language select — `aria-label` + i18n (en/sr/de).
4. **Ops health** `role="status" aria-live="polite"`.
5. **Kontrast**: `--text-muted-dark` podignut na `#8B9CB3`; staff hard `#64748B` → token.

## Testovi

- `tests/unit/poglavlje-7-4-a11y.test.mjs`

## Ocena P7 posle 7.4

**~8.5/10** — WCAG AA nije „zatvoren audit“, ali skip/focus/aria hotspots na glavnim surface-ima jesu.

## Sledeće

**P8.1** — full unit + rules + firebase isolation + build.
