# FAZA 3 D24.1.1 — Security / Transaction Closeout Correction

**Datum:** 2026-08-09  
**Base SHA:** `a6fbcb508c67287c33479f38c3678cd44684ee60`  
**Polazna tačka:** dirty D24.1 tree (bez reseta)  
**STOP:** bez D24.2 uniqueness, bez nove kolekcije/dependency, bez commit/push/deploy, bez budget bump, bez Phase 4

## Sažetak

Zatvoreni su preostali D24.1 propusti: LIVE staff fail-closed u assignment transakciji, driver group drift + inactive, null credential-key migracija, dirty profile read boundary, group existence u CA create tx, executable proofs, fresh visual trail.

Sve mutacije ostaju server-authoritative i fail-closed.

## Gate rezultati

| Gate | Rezultat | Exit |
|------|----------|------|
| secrets | OK | 0 |
| lint | OK | 0 |
| full unit | **762** pass (≥757) | 0 |
| HTTP/auth | 34 pass (production CA route + staff/driver HTTP) | 0 |
| full Rules emulator | **146** pass (≥137); D24.1.1 closeout svi green | 0 |
| full E2E | **105** pass (`--workers=1`) | 0 |
| firebase isolation | OK | 0 |
| npm audit --omit=dev | 0 vulnerabilities | 0 |
| clean build + D17 | staff **577756** ≤ 581632; translations **343352** ≤ 377856 | 0 |
| jezici | tačno `de,en,sr` + nove ops_* ključeve | 0 |
| visual trail | `reports/phase-3-d2411-visual-2026-08-09T19-05-05/` | 0 |
| manifest verifier | VERIFIER_OK (posle pack) | 0 |

## Funkcionalni closeout

### 1. LIVE staff fail-closed
U assignment `runTransaction`: staff doc mora postojati; `active !== false`; role `dispatcher`; LIVE `groups` jedini autoritet — **nema** fallbacka na `req.staff.groups`. Delete / deactivate / role-change tokom mutation prozora → `403 STAFF_SESSION_INVALID`, zero writes.

### 2. Driver group drift + active
Day-lock grupa sačuvana kao `lockedGroupId`. LIVE home group mora ostati ista → inače `409 DRIVER_SCOPE_CHANGED`. Nova dodela neaktivnom → `409 DRIVER_INACTIVE`. `clear` dozvoljen za uklanjanje postojeće dodele (emulator dokaz).

### 3. Null credential keys
Rules + migration + server detection koriste **key ownership**. `eid: null` / `loginCodeHash: null` su migration kandidati; null se ne kopira u credentials; ključ se uklanja iz profila; `legacyCredentialProfiles` ih prijavljuje; counters tačni na tx retry. Emulator: dirty-null → blocked → clean apply → readable. **Bez live migration apply.**

### 4. Dirty profile read boundary
Credential-bearing profil: deny za Dispo, own-driver, SuperAdmin browser. CA own-tenant dozvoljen radi migracije. `driver_credentials` Admin SDK only.

**Budući rollout (ne sada):** clean server writes → dry-run → backup/verify → apply migration → zero-dirty verification → Rules deploy.

### 5. Group existence u create tx
CA manual create čita home/known grupe u istoj transakciji pre write-ova. Brisanje grupe tokom mutation prozora → `group-not-found`, bez orphan profila/credentials.

### 6. Executable proofs
Fail-first + final: staff deleted/inactive/role-changed; group drift; inactive assign; clear inactive; null-key Rules; dirty own-driver/SA deny; group deletion race (in-tx hook); bus concurrency flip after tx read (ne pre-request); CA auth na **pravom** `registerCompanyAdminDriverRoutes` handleru.

### 7. Visual
Nov folder: `reports/phase-3-d2411-visual-2026-08-09T19-05-05/`  
Screenshotovi su **UI dokaz**, ne Rules/auth/tx dokaz (README + TRAIL.json).

## Deliverables

- Report: `reports/phase-3-d2411-report-2026-08-09.md`
- Ledger: `reports/phase-3-d2411-change-ledger.md`
- Logs: `reports/phase-3-d2411-logs/`
- Visual: `reports/phase-3-d2411-visual-2026-08-09T19-05-05/`
- Manifest: `reports/phase-3-d2411-source-manifest.txt`
- Review ZIP / Full ZIP: vidi `phase-3-d2411-logs/pack.txt`

## STOP

D24.1.1 zatvoren. Ne pokretati uniqueness guard, D24.2, ni Phase 4.
