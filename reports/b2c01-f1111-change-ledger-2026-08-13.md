# B2C-01-F1.1.1.1 change ledger — 2026-08-13

Baseline HEAD: `b1d057a74e5fc7a55ba55e3bcb6720372871631f`  
Branch: `staging/phase-3-isolation`  
Staged: **0** · No commit/push/PR/deploy · Dirty F1/F1.1/F1.1.1 tree preserved

## Production

| File | ŠTA | ZAŠTO | DONOSI |
|------|-----|-------|--------|
| `js/admin/superadmin.js` | Close uses `getSaCreateFlowIfLoaded` → flow close if loaded, else sync `dismissSaCreateModalShellLocal` (clears password); loader toast ownership `data-sa-create-loader-toast`; clear on successful load; remove `saDashboardUsesLocalState` / `window.USE_LOCAL_STATE` override; Escape shell when unloaded | Close must not start chunk/recovery/toast; loader toast must not wipe unrelated/outcome toasts; production decision uses imported `USE_LOCAL_STATE` | Close no-load; one loader toast lifecycle; clean production surface |
| `js/admin/sa-create-company-flow.js` | `saUsesLocalState()` returns imported `USE_LOCAL_STATE` only | No mutable window override in create-flow | Production API vs local path follows real runtime config |
| `js/admin/sa-create-company-flow-loader.js` | Pure `createSaCreateCompanyFlowLoader()` factory; remove `__set*` / `__reset*` ForTests exports | Unit tests must not require production hooks | Pure-function loader tests; no prod test API |

## Tests

| File | ŠTA | ZAŠTO | DONOSI |
|------|-----|-------|--------|
| `tests/unit/b2c01-f1111-create-company-ca.test.mjs` | **ADD** Close/loader/hook/pure-factory contracts | Fail-first/final source proofs | Unit gate |
| `tests/unit/b2c01-f111-create-company-ca.test.mjs` | Align visual path + hook guards to F1.1.1.1 | Shared visual trail evolved | No stale 06/07 expectations |
| `tests/unit/b2c01-f11-create-company-ca.test.mjs` | Pure loader factory (no ForTests) | Match production loader | Unit green |
| `tests/e2e/b2c01-f1-create-company-ca.spec.js` | Remote boot (harness off) + Firebase CDN route stub; Close/toast/retry; execution error; no ForTests | Prove production API path without window override | 11 E2E tests green |
| `tests/e2e/b2c01-f1-visual-trail.spec.js` | `b2c01-f1111-visual` 01–04 + measured TRAIL | Owner visual mandate | Absolute Windows paths |

## Explicitly unchanged

- API / Rules / schema / dependencies  
- B2C-01-R1, B2C-03, H1-B/H1-C, Phase 4  
- BLAGUSS / staging QA tenant data  
- Bundle budget ceilings  
- `main-staff.js` mirror of imported `USE_LOCAL_STATE` (not an override)

## Review patch

`reports\b2c01-f1111-review.patch` — Node Buffer, mojibake scan clean.
