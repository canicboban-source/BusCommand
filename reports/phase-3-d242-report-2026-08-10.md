# FAZA 3 D24.2 — Concurrency-safe driver uniqueness

**Datum:** 2026-08-10  
**Base SHA:** `a6fbcb508c67287c33479f38c3678cd44684ee60`  
**Polazna tačka:** dirty D24.1.1.1 tree (bez reseta)  
**STOP:** bez Phase 4 / commit / push / deploy / budget bump

## Šta / zašto / šta donosi

**Šta:** Jedan fiksni tenant-scoped, server-only guard dokument  
`companies/{companyId}/ops/driver_identity_guard` (`revision`, `updatedAt` only).  
CA manual create i CSV/import create učestvuju u istom Firestore tx ugovoru:  
LIVE guard → license/status/`maxDrivers` → grupe → EID/`company_code` uniqueness →  
profil + credentials + guard bump (sva čitanja pre write-ova).

**Zašto:** Paralelni create/import preko različitih novih doc ID-eva mogao je  
duplirati EID / `company_code` / poslednje `maxDrivers` mesto (posebno non-tx CSV).

**Šta donosi:** Fail-closed paralelizam (jedan success), zero orphan writes,  
retry-safe revision, browser deny na guard, error payload bez otkrivanja  
EID/`company_code`/tuđeg `driverId`.

## Identitet / license semantika

| Dimenzija | Kanonsko polje | Conflict code |
|-----------|----------------|---------------|
| Employee id | `driver_credentials.eid` | `EID_EXISTS` |
| Company code (Firmencode) | `driver_credentials.companyCodeHash` | `COMPANY_CODE_EXISTS` (stabilizacija postojećeg 409) |
| Capacity | `settings/main` via `resolveLicenseSnapshot` | `DRIVER_LIMIT_REACHED` |

Nema novog driver “license number” schema polja. EID i dalje samo u credentials.

## Fail-first → final

| Dokaz | Log | Exit |
|-------|-----|------|
| Fail-first: legacy non-tx parallel import same EID → 2 duplicates | `fail-first-race.txt` | **1** |
| Final A–H concurrency + Rules browser deny | `final-concurrency.txt` | **0** |

## Gateovi

| Gate | Rezultat | Exit |
|------|----------|------|
| secrets | OK | 0 |
| lint | OK | 0 |
| unit | **767** (≥762) | 0 |
| HTTP/auth (CA drivers) | 5 pass | 0 |
| Rules | **154** (≥146) | 0 |
| E2E | **105** (`--workers=1`) | 0 |
| isolation | OK (in build) | 0 |
| audit --omit=dev | 0 vulns | 0 |
| languages | `sr,en,de` | 0 |
| build + D17 | staff **577956** ≤ 581632; plan-import lazy chunk | 0 |
| visual | `phase-3-d242-visual-2026-08-10T14-01-39` | 0 |

## Visual (UI-only)

1. CA create success  
2. Localized duplicate EID/license — no secret values  
3. CSV/import conflict  
4. maxDrivers block  

Screenshots **ne** dokazuju transakciju, Rules ili authz.

## Deliverables

| Artifact | Path / value |
|----------|----------------|
| Report | `reports/phase-3-d242-report-2026-08-10.md` |
| Ledger | `reports/phase-3-d242-change-ledger.md` |
| Logs | `reports/phase-3-d242-logs/` |
| Visual | `reports/phase-3-d242-visual-2026-08-10T14-01-39/` |
| Manifest | `reports/phase-3-d242-source-manifest.txt` |
| body-sha256 | see manifest footer / `reports/phase-3-d242-logs/pack.txt` |
| Review ZIP | `reports/phase-3-d242-review-source-2026-08-10.zip` |
| Full ZIP | `reports/phase-3-d242-full-deliverable-2026-08-10.zip` |

Review ⊆ full byte-identical: **pass** (`VERIFIER_OK`). Nested ZIP / backslash / duplicates: **0**.

## STOP

D24.2 zatvoren. Ne pokretati Phase 4 / commit / push / deploy bez owner odluke.
