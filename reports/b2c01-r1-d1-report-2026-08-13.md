# B2C-01-R1-D1 — Read-only refresh recovery diagnostic — 2026-08-13

## Verdict: **NEEDS_NEW_API_APPROVAL**

Recommended path: **Option B** — narrow Super-Admin-only “create missing company admin” endpoint (no new schema), plus Manage account CTA gated on server truth.  
**Not implemented in this task.**

Evidence mode: **READ-ONLY CODE/CONTRACT DIAGNOSTIC**  
Real writes = **0** · no live Auth/Firestore/API mutations · BLAGUSS / staging QA tenant untouched · source/test/config unchanged

---

## Identity

| Item | Value |
|------|-------|
| Workspace | `C:\Users\cane\Desktop\BusCommand-ca-monthly-import` |
| Branch | `staging/phase-3-isolation` |
| HEAD | `b1d057a74e5fc7a55ba55e3bcb6720372871631f` |
| staged | **0** |
| Dirty B2C-01 tree | preserved (no reset/stash/clean) |

---

## 1. Server truth

### Endpoints that feed UI

| Surface | Endpoint | Source of CA list |
|---------|----------|-------------------|
| Companies table | `GET /api/admin/companies` + `GET /api/admin/company-admins` | Firestore `companies/*/users` where `role == company_admin` via `listAllCompanyAdmins` |
| Manage account | `GET /api/admin/company/:companyId` | `getCompanyDetail` → `admins[]` from same users collection |

### Can today’s responses distinguish?

| State | Distinguished today? | How / gap |
|-------|----------------------|-----------|
| No CA at all | **Partial** | Empty `admins` / no active join — also true if GET failed and state wiped |
| Active CA | **Yes** | `active !== false` in list / detail |
| Inactive CA | **Yes in detail** | Shown Disabled; table join prefers active only → table may show “No company admins” while inactive exists |
| Auth exists, Firestore user missing | **No** | Not probed |
| Firestore user exists, Auth missing | **No** as class | Admin row may show; Reset → 404 |
| API error / unknown | **Weak** | Table may keep stale `window.state.companyAdmins` or empty array without explicit unknown |

### “No company admins created” basis

- **Table:** local join on `window.state.companyAdmins` after successful GET (or stale/empty on failure) — **not** a typed server enum.
- **Manage account empty copy:** `company.admins.length === 0` from last successful `getCompanyDetail` — authoritative for **Firestore docs only**.

---

## 2. Existing create-user tok

See full diagram:  
`C:\Users\cane\Desktop\BusCommand-ca-monthly-import\reports\b2c01-r1-d1-call-flow.md`

| Question | Finding |
|----------|---------|
| SA-only for company_admin? | **Yes in practice** — `requireUserProvisioner` then route **403** if actor is `company_admin` |
| Validates company exists? | **Yes** — before Auth create |
| Checks company has no CA? | **No** |
| Parallel two requests? | **Both can succeed** → duplicate CAs |
| Auth OK, Firestore fail? | Compensation deletes Auth (+FS doc if written); else `compensation-failed` |
| Safe compensation? | **Yes when cleanup succeeds**; failure is explicit error code |
| Retry duplicate? | Email collision → **409** `"Email već postoji."`; **different email → second CA** |
| Cross-tenant leak in errors? | **Low** — 409 has no companyId; 404 company is generic |
| Can endpoint safely serve R1 without new schema? | **Only with new uniqueness semantics** — current contract is insufficient for fail-closed “missing only” |

---

## 3. Manage account UI

| Item | Finding |
|------|---------|
| Render | `#sa-detail-admins` via `renderCompanyDetailAdmins` in `js/admin/superadmin.js` |
| Actions today | Reset password; Enable/Disable — **only when admins exist** |
| Create missing CA | **Absent** — empty state is text only (`sa_detail_no_admins`) |
| Create vs Reset | Reset requires existing `admin.id`; Create must appear **only** when server says `missing` |
| Hide Create when inactive/unknown/inconsistent | Required; today no Create at all |
| Reuse modal for name/email/password | **Yes** — existing detail modal can host a small form under admins section; no new modal required |
| a11y / responsive | Modal already dialog; new fields must follow focus trap / labels; keep within existing detail layout |

---

## 4. Refresh recovery contract (R1 must obey)

After refresh, only **server GET detail** may authorize Create.

**Forbidden:** pending CA in storage; password persistence; credential query params; session assumption; re-`createCompany`; company-create 409 as “missing CA”; Create without positive `missing`.

---

## 5. Authz / privacy (future tok)

| Rule | Plan |
|------|------|
| companyId | From Manage account open context (`_pendingDetailCompanyId` / path param), **not** editable free-text trusted as authority |
| No SA tenant ops expansion | Endpoint only provisions CA user doc + Auth claims — no plans/drivers/buses/shifts |
| Password | Ephemeral form field → POST body only; never toast/storage/log/trail |
| Other-firm Auth email | Keep 409 generic; no existence oracle beyond current create-user behavior |
| Rules / schema | **No change** under Option B |
| Audit | Must write truthful create (or dedicated) event with actor + companyId + uid |

---

## 6. Failure matrix

Full matrix:  
`C:\Users\cane\Desktop\BusCommand-ca-monthly-import\reports\b2c01-r1-d1-failure-matrix.md`

---

## 7. Options comparison

| Option | Security | Concurrency | Recovery | Code scope | Bundle | Schema/API | Risk |
|--------|----------|-------------|----------|------------|--------|------------|------|
| **A** Existing `createUser` + Manage orchestration | Medium — needs new uniqueness guard inside shared provisioner | Weak today; fix via tx check | UI can gate on empty admins | Small–medium UI + provisioner change | Low if inline | API behavior change, no new route | Shared endpoint semantics / race if guard weak |
| **B** Narrow `create-missing-admin` (no schema) | **High** — SA-only, path companyId, explicit missing check | Strong with transactional zero-CA | Clear refresh re-GET | Medium (route + UI) | Low–medium | **New API** (approval) | Best isolation |
| **C** Reservation/operation schema | High | Highest | Excellent cross-refresh | Large | Higher | **Schema + API** | Overkill for R1 |
| **D** Detail-only `caProvisionState` + tightened A | High if Auth probe careful | Depends on provisioner tx | Better CTA gating | Medium | Low | Soft API field + behavior | Auth orphan detection still hard |

### Recommendation (not implemented)

**Option B** — `POST /api/admin/company/:companyId/create-missing-admin` + Manage account form when detail says `missing`.  
Reuse `provisionUser` internals **after** transactional “no company_admin docs” check (active **or** inactive → deny).  
Optional response field on GET detail: `caProvisionState` for honest CTA gating.

---

## 8. Fail-first plan for future R1

Executable proofs **before** implementation (must go red):

1. Refresh → company with zero Firestore CA → Manage account has **no** functional create recovery.  
2. Network: `createCompany` count stays **0** on recovery attempts.  
3. Two parallel CA submits → not two accounts (today likely fails this — documents need for B).  
4. Forced unknown/inconsistent → no Create CTA.  
5. Password absent from toast/storage/log/TRAIL.  
6. After future success: Companies table shows CA after refresh.  
7. Every visible CTA has asserted outcome.  
8. No production `__get/__set/__reset` / USE_LOCAL_STATE override hooks.

---

## D17 estimate (future R1)

Current staff headroom (post F1.1.1.1-A build): ~**10.6 KiB** (≥ 8 KiB required).

| Item | Estimate |
|------|----------|
| JS delta if inline in `superadmin.js` | ~1.5–3 KiB raw |
| Safer: tiny lazy chunk for missing-CA form | keeps staff headroom margin |
| translations | ~4–8 keys × sr/en/de only; keep translations under budget |
| Budget bump | **Forbidden** |
| Languages | **exactly** sr/en/de |

---

## Artifacts (absolute Windows paths)

- `C:\Users\cane\Desktop\BusCommand-ca-monthly-import\reports\b2c01-r1-d1-report-2026-08-13.md`
- `C:\Users\cane\Desktop\BusCommand-ca-monthly-import\reports\b2c01-r1-d1-call-flow.md`
- `C:\Users\cane\Desktop\BusCommand-ca-monthly-import\reports\b2c01-r1-d1-failure-matrix.md`
- `C:\Users\cane\Desktop\BusCommand-ca-monthly-import\reports\b2c01-r1-d1-change-ledger-2026-08-13.md`
- `C:\Users\cane\Desktop\BusCommand-ca-monthly-import\reports\b2c01-r1-d1-logs\`
- `C:\Users\cane\Desktop\BusCommand-ca-monthly-import\reports\b2c01-r1-d1-visual\TRAIL.json`

---

## STOP

No R1 implementation · no B2C-03 / H1-B / H1-C / Phase 4 · no code/test/config change · no stage/commit/push/PR/deploy/workflow.
