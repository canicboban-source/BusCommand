# Dependencies
node_modules/
npm-debug.log*

# Firebase / tajni ključevi — NIKAD ne uploada na GitHub!
firebase-admin-key.json
firebase-admin-key.json.json
*.json.json
serviceAccount*.json
*-credentials.json

# OS fajlovi
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
.claude/
# Ignore Cursor local state; keep committed project rules
.cursor/*
!.cursor/rules/
!.cursor/rules/**
*.swp
*.swo

# Env (tajne)
.env
.env.local
.env.*
!.env.example

# Local toolchains (portable Node, etc.)
.tools/

# Temp / build
dist/
build/
.cache/
test-results/
playwright-report/
/buscommand.zip
firestore-debug.log
firebase-debug.log
.firebase/

# Generated local evidence (keep files on disk; do not commit)
/reports/**/*.zip
/reports/**/*-logs/
/reports/**/*-visual*/
/reports/**/*.log
/reports/**/*.patch
/reports/**/*.tmp
/reports/**/*-git-status-short.txt
/reports/**/*-git-diff-stat.txt
/reports/**/*-source-manifest.txt
